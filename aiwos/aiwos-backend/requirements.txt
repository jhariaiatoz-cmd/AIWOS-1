# ── Web framework & server ──────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0          # [standard] pulls in uvloop, httptools, websockets
python-multipart==0.0.12           # required by FastAPI for UploadFile / form data

# ── Database ORM & drivers ──────────────────────────────────────────────────
SQLAlchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0                    # async PostgreSQL driver for SQLAlchemy async engine
psycopg2-binary==2.9.9             # sync driver used by Alembic offline migrations
pgvector==0.3.3                    # pgvector extension type for SQLAlchemy

# ── Data validation & settings ──────────────────────────────────────────────
pydantic==2.11.7
pydantic-settings==2.5.2
email-validator==2.2.0             # required by pydantic for EmailStr
python-dotenv==1.0.1               # .env file loading via pydantic-settings

# ── Authentication ──────────────────────────────────────────────────────────
bcrypt==5.0.0
PyJWT==2.13.0                      # imported as `jwt`

# ── LLM providers ───────────────────────────────────────────────────────────
anthropic==0.34.0
openai==1.50.0
google-genai==2.8.0

# ── Document parsing (knowledge file extraction) ────────────────────────────
pypdf==4.3.0
python-docx==1.1.2

# ── Timezone data ───────────────────────────────────────────────────────────
tzdata==2026.2                     # cross-platform tz database (needed on Linux containers)
